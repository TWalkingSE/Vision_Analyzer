# ============================================
# Vision Analyzer v4.0 - Dependencies
# ============================================
# Python 3.10+ required
# Este ciclo de hardening nao adicionou novas dependencias obrigatorias,
# mas passou a declarar pisos/faixas para tornar o ambiente mais previsivel.

# Core AI Libraries
openai>=1.30.0,<2.0.0
ollama>=0.2.0,<1.0.0

# Image Processing
Pillow>=10.0.0,<12.0.0
pillow-heif>=0.16.0
numpy>=1.24.0,<3.0.0
opencv-python-headless>=4.8.0,<5.0.0
tqdm>=4.66.0,<5.0.0

# RAW Image Support
rawpy>=0.19.0
imageio>=2.34.0

# Metadados EXIF / GPS
# OBRIGATÓRIO: sem piexif, `image_preprocessor._extract_exif` não extrai as coordenadas
# GPS nem os links de mapa, e `exif_analyzer` perde a checagem de GPS — sem fallback e
# sem erro visível. Câmera e data continuam vindo via Pillow, então a falta passa
# despercebida justamente no dado mais sensível de uma análise forense.
piexif>=1.1.3

# Environment Management
python-dotenv>=1.0.0,<2.0.0

# Web Interface
# Piso 1.50.0: a UI usa width='stretch' em st.button, st.image e st.dataframe.
#   - st.button ganhou `width` (aceitando "stretch") no 1.48.0
#   - st.image e st.dataframe só passaram a aceitar "stretch" no 1.50.0
#     (em 1.48/1.49 o parâmetro ainda era `int | None`)
# 1.50.0 é, portanto, a primeira versão em que todos os três funcionam. Abaixo disso a UI
# quebra com "TypeError: ButtonMixin.button() got an unexpected keyword argument 'width'".
streamlit>=1.50.0,<2.0.0

# OCR via Ollama (não requer pacotes Python adicionais)
# Use: ollama pull glm-ocr:bf16
# Use: ollama pull richardyoung/olmocr2:7b-q8

# ============================================
# Exportação de Relatórios
# ============================================
# PDF Export
reportlab>=4.0.0,<5.0.0

# DOCX Export (Word)
python-docx>=1.1.0,<2.0.0

# Testes
pytest>=8.0.0,<10.0.0

# ============================================
# PyTorch com CUDA (Aceleração GPU)
# ============================================
# O pip install padrão instala a versão CPU-only.
# Para habilitar GPU, desinstale e reinstale com o índice CUDA correto:
#
#   pip uninstall torch torchvision torchaudio -y
#
# RTX 30xx (Ampere)  — CUDA 12.6:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# RTX 40xx (Ada Lovelace) — CUDA 12.6 ou 12.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# RTX 50xx (Blackwell) — CUDA 12.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Consulte https://pytorch.org/get-started/locally/ para outras versões.
torch>=2.6.0,<3.0.0
torchvision>=0.21.0,<1.0.0
torchaudio>=2.6.0,<3.0.0

# ============================================
# Detecção de Objetos
# ============================================
# YOLO11 - Detecção rápida e leve
ultralytics>=8.0.0,<9.0.0

# Detectron2 - Detecção avançada para cenas complexas (OPCIONAL)
# Sem suporte oficial para Windows — veja o README para instruções detalhadas.
# Linux/WSL2: pip install 'git+https://github.com/facebookresearch/detectron2.git'
# Windows: requer build manual ou WSL2 (consulte README seção "Instalação do Detectron2")
# detectron2

# ============================================
# Busca Semântica (OPCIONAL)
# ============================================
# chromadb>=0.5.0
# Usa embeddings locais via Ollama (pacote `ollama` ja incluido acima)

# ============================================
# Cores Dominantes (OPCIONAL)
# ============================================
# Melhora a extração de cores dominantes usando K-means em vez da contagem de
# frequência. Há fallback em numpy quando ausente, então instalar é opcional.
# scikit-learn>=1.4.0
